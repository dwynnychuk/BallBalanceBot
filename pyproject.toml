[project]
name = "ballbalancebot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dwynnychuk", email = "dwynnychuk@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
ballbalancebot = "ballbalancebot:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
